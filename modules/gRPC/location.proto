syntax = "proto3";

message Location {
  
  count = 0
  while count < 100 : 
  	enum Loc {
   	 id = 0;
    	 longitude = 
         Latitude = 2;
         PersonId = 
  }


  string id = 1;
  string created_by = 2;
  Status status = 3;
  string created_at = 4;
  repeated Equipment equipment = 5;
}

message Empty {

}

message Locations {
  repeated Location orders = 1;
}

service LoccationService {
    rpc Create(Location) returns (Location);
    rpc Get(Location) returns (Locations);
}
